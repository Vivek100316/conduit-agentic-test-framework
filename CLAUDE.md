# Contributing to this repository

@AGENTS.md

Everything above is imported from [AGENTS.md](AGENTS.md), which is the canonical file.

It lives there rather than here because `AGENTS.md` is the cross-tool convention, so one
copy serves Claude Code and every other agent. The two files used to be maintained in
parallel and told to stay identical "in substance" — which is a drift risk dressed up as a
convention. One file, imported, cannot drift.
